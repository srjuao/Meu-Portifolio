{
  "name": "fluxoapps-landing",
  "version": "1.0.0",
  "private": true,
  "description": "Landing page Fluxoapps (HTML/CSS/JS estático)",
  "scripts": {
    "start": "serve -s . -l tcp://0.0.0.0:${PORT:-3000} --no-clipboard"
  },
  "dependencies": {
    "serve": "^14.2.4"
  },
  "engines": {
    "node": ">=18"
  }
}
